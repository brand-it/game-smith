output: stdout
jobs:
  pid_liveness:
    run: "pid_liveness"
    schedule: "every 30 seconds"
    output: silent
    run_on_start: true
    tags: ["maintenance"]
  log_cleanup:
    run: "log_cleanup"
    schedule: "every 1 hours"
    output: silent
    tags: ["maintenance"]
